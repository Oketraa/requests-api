basePath: /
definitions:
  models.CreateRequestInput:
    properties:
      description:
        type: string
      title:
        type: string
    type: object
  models.Request:
    properties:
      created_at:
        type: string
      description:
        type: string
      id:
        type: integer
      status:
        type: string
      title:
        type: string
      updated_at:
        type: string
    type: object
  models.UpdateRequestInput:
    properties:
      description:
        type: string
      status:
        type: string
      title:
        type: string
    type: object
host: localhost
info:
  contact: {}
  description: Backend для управления заявками
  title: Requests API
  version: "1.0"
paths:
  /api/requests:
    get:
      description: Возвращает список заявок
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/models.Request'
            type: array
      summary: Получить список заявок
      tags:
      - requests
  /api/requests/{id}:
    delete:
      description: Удаляет заявку по ID
      parameters:
      - description: ID заявки
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties:
              type: string
            type: object
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Удалить заявку
      tags:
      - requests
    get:
      description: Возвращает одну заявку по её идентификатору
      parameters:
      - description: ID заявки
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.Request'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Получить заявку по ID
      tags:
      - requests
    put:
      consumes:
      - application/json
      description: Обновляет заявку по ID
      parameters:
      - description: ID заявки
        in: path
        name: id
        required: true
        type: integer
      - description: Новые данные
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/models.UpdateRequestInput'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.Request'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Обновить заявку
      tags:
      - requests
  /api/requests/create:
    post:
      consumes:
      - application/json
      description: Создает новую заявку
      parameters:
      - description: Данные заявки
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/models.CreateRequestInput'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.Request'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Создать заявку
      tags:
      - requests
swagger: "2.0"
